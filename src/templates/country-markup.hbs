<h2 class='country-title'>{{name}}</h2>

<div class='country-card'>
    <div class='country-info'>
        <ul>
            <li><span>Сталица:</span> {{capital}}</li>
            <li><span>Население:</span> {{population}}</li>
            <li><span>Язык:</span>
                <ul>
                    {{#each languages}}
                    <li>
                        {{name}}
                    </li>
                    {{/each}}
                </ul>

            </li>
        </ul>
    </div>
    <img src='{{flag}}' alt='Flag of {{name}}' width="300">
</div>